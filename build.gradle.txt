android {
    compileSdkVersion 34
    defaultConfig {
        applicationId "com.legalroyale.game"
        minSdkVersion 21          // Android 5.0+ (WebM audio support)
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }
    buildTypes {
        release {
            minifyEnabled false   // Jangan minify (JS game bisa rusak)
            shrinkResources false
        }
    }
    // KRITIS: Pastikan assets ter-include
    sourceSets {
        main {
            assets.srcDirs = ['src/main/assets']
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}